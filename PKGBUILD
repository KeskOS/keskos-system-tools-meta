pkgname=keskos-system-tools-meta
pkgver=0.1.0
pkgrel=1
pkgdesc="Meta package for curated KeskOS system utilities and customization tools"
arch=(any)
url="https://github.com/memegeko/keskos"
license=(GPL-3.0-or-later)
depends=(
  btop
  fastfetch
  gparted
  htop
  kate
  kdeconnect
  papirus-icon-theme
  partitionmanager
  qt6ct
  timeshift
)
source=()
sha256sums=()

package() {
  :
}
